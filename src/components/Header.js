import { useState } from 'react';

export const Header = () => {
  //   const [header, setHeader] = useState([]);

  return (
    <header className='header'>
      <h1>nc news</h1>
    </header>
  );
};
